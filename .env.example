# YKS — Environment Variables
# Copy to .env and fill real values for production.
#
# NOTE: Brand name, domain, social links and analytics now live in
# src/config/site.ts (the single source of truth). Set those there.
# This file is only for secrets / build-time values.

# Lemon Squeezy (for paid premium packs — server verification / future webhooks)
LEMONSQUEEZY_API_KEY=your_ls_api_key_here
LEMONSQUEEZY_STORE_ID=your_store_id

# Netlify (auto-injected in production) — no secrets needed for static + Forms
